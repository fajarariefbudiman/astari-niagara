{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "npm install && npm run build && artisan config:cache && php artisan route:cache && php artisan migrate --force && php artisan storage:link && php -S 0.0.0.0:$PORT -t public"
  }
}
