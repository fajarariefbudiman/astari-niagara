{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "npm run build && php artisan config:cache && php artisan migrate --force && php artisan storage:link && php -S 0.0.0.0:$PORT -t public"
  }
}
